# 🧪 Тесты ENASGame Bot

## Структура тестов

```
tests/
├── __init__.py
└── test_team_name_validator.py  # Валидация названий команд (48 тестов)
```

## Запуск тестов

### Все тесты:
```bash
python -m unittest discover tests -v
```

### Конкретный файл:
```bash
python -m unittest tests.test_team_name_validator -v
```

### С покрытием кода (если установлен coverage):
```bash
pip install coverage
coverage run -m unittest discover tests
coverage report
coverage html  # создаст htmlcov/index.html
```

## Тестовое покрытие

### `test_team_name_validator.py` (48 тестов)

**Что тестируется:**

✅ **Корректные названия** (8 тестов):
- Английские, русские, смешанные языки
- С цифрами, дефисами, подчёркиваниями, точками
- Минимальная и максимальная длина

✅ **Проверка длины** (4 теста):
- Слишком короткие (< 3 символа)
- Слишком длинные (> 50 символов)
- Пустые строки и пробелы

✅ **Спецсимволы** (13 тестов):
- Недопустимые: @, #, $, %, &, *, +, =, /, \, [], (), эмодзи
- Разрешённые: дефис, точка, подчёркивание

✅ **Минимум букв** (3 теста):
- Только цифры (запрещено)
- Только 1 буква (запрещено)
- Минимум 2 буквы (разрешено)

✅ **Запрещённые бренды** (12 тестов):
- Team Liquid, Navi, FaZe Clan, G2 Esports, Fnatic
- Cloud9, T1, OG, Team Secret, Virtus Pro, NiP
- Регистронезависимая проверка
- Бренды в середине названия

✅ **Граничные случаи** (5 тестов):
- Похожие на бренды названия (разрешены)
- None как входное значение
- Обрезка пробелов
- Вспомогательные функции

## Статистика

- **Всего тестов:** 48
- **Успешно:** 48 ✅
- **Провалено:** 0 ❌
- **Время выполнения:** ~0.06 сек

## Добавление новых тестов

1. Создайте файл `test_*.py` в папке `tests/`
2. Импортируйте `unittest`
3. Создайте класс, наследующий `unittest.TestCase`
4. Напишите методы начинающиеся с `test_`

Пример:
```python
import unittest

class TestMyFeature(unittest.TestCase):
    def test_something(self):
        self.assertEqual(1 + 1, 2)
```

## CI/CD

Тесты можно интегрировать в GitHub Actions:

```yaml
name: Tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - run: python -m unittest discover tests -v
```
