# ⚡ Быстрый старт: Запуск бота на сервере за 5 минут

## Шаг 1: Подключение к серверу

```bash
ssh root@ваш_IP_адрес
```

## Шаг 2: Автоматическая установка

```bash
curl -o setup.sh https://raw.githubusercontent.com/vsbaha/ENASGame_bot_2025/main/deployment/setup_server.sh
chmod +x setup.sh
sudo bash setup.sh
```

✅ Скрипт автоматически установит все необходимое

## Шаг 3: Настройка переменных окружения

```bash
nano /home/ENASGame_bot_2025/.env
```

Измените эти строки:

```env
BOT_TOKEN=7123456789:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw    # ← Ваш токен от @BotFather
ADMIN_IDS=123456789,987654321                              # ← Ваш Telegram ID
ADMIN_CHAT_ID=-1001234567890                               # ← ID админ-чата (опционально)
CHALLONGE_API_KEY=abc123def456                             # ← API ключ Challonge
CHALLONGE_USERNAME=enas_tournaments                        # ← Username Challonge
```

**Сохранить:** `Ctrl+O` → `Enter` → `Ctrl+X`

## Шаг 4: Запуск бота

```bash
sudo systemctl start tournament_bot
sudo systemctl enable tournament_bot
```

## Шаг 5: Проверка

```bash
sudo systemctl status tournament_bot
```

Если статус **active (running)** → **✅ Готово!**

---

## 🔍 Как получить токен бота?

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Придумайте имя: `ENAS Tournament Bot`
4. Придумайте username: `enas_tournament_bot`
5. Скопируйте токен (выглядит так: `7123456789:AAHdqTcvCH1vGWJxfSeofSAs0K5PALDsaw`)
6. Вставьте в `.env` файл

## 🔍 Как узнать свой Telegram ID?

1. Откройте [@userinfobot](https://t.me/userinfobot)
2. Нажмите "Start"
3. Скопируйте **Id**: `123456789`
4. Вставьте в `.env` как `ADMIN_IDS=123456789`

## 🎯 Как получить Challonge API ключ?

1. Зайдите на [challonge.com](https://challonge.com)
2. Зарегистрируйтесь или войдите
3. Перейдите в **Settings** → **Developer API**
4. Скопируйте ваш **API Key**
5. Вставьте в `.env` файл

---

## 📋 Полезные команды

### Просмотр логов (реального времени)
```bash
sudo journalctl -u tournament_bot -f
```

### Перезапуск бота
```bash
sudo systemctl restart tournament_bot
```

### Остановка бота
```bash
sudo systemctl stop tournament_bot
```

### Обновление бота после изменений в GitHub
```bash
cd /home/ENASGame_bot_2025
sudo bash deployment/deploy.sh
```

### Мониторинг состояния
```bash
cd /home/ENASGame_bot_2025
bash deployment/monitor.sh
```

### Создание бэкапа БД
```bash
cd /home/ENASGame_bot_2025
bash deployment/backup.sh
```

---

## ❌ Проблемы?

### Бот не запускается

```bash
# Смотрите логи
sudo journalctl -u tournament_bot -n 50

# Проверьте .env файл
cat /home/ENASGame_bot_2025/.env

# Попробуйте запустить вручную для отладки
cd /home/ENASGame_bot_2025
source venv/bin/activate
python main.py
```

### Ошибка "Invalid token"

- Проверьте токен в `.env` файле
- Убедитесь что токен скопирован полностью
- Проверьте что нет лишних пробелов

### Бот не отвечает в Telegram

- Проверьте что бот запущен: `systemctl status tournament_bot`
- Проверьте логи: `journalctl -u tournament_bot -f`
- Проверьте интернет на сервере: `ping google.com`

---

## 📊 Контрольный чеклист

- [ ] Арендован сервер (Ubuntu 20.04+)
- [ ] Подключение по SSH работает
- [ ] Скрипт `setup_server.sh` запущен успешно
- [ ] Файл `.env` настроен с реальными токенами
- [ ] Получен токен от @BotFather
- [ ] Узнан свой Telegram ID
- [ ] Получен Challonge API ключ (если нужно)
- [ ] Бот запущен: `systemctl start tournament_bot`
- [ ] Статус активен: `systemctl status tournament_bot`
- [ ] Логи без ошибок: `journalctl -u tournament_bot -f`
- [ ] Бот отвечает в Telegram
- [ ] Автозапуск включён: `systemctl enable tournament_bot`

**Если всё отмечено ✅ - бот готов к работе!** 🎉

---

## 📚 Дополнительная документация

- [Полная инструкция по деплою](README.md)
- [Устранение неполадок](README.md#устранение-неполадок)
- [Настройка безопасности](README.md#безопасность)
- [Резервное копирование](README.md#резервное-копирование)
