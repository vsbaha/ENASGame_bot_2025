# Исправление ошибки итерации по символам каналов

## Проблема

Пользователь сообщил:
1. При попытке регистрации показывало "2 канала с пустым названием"
2. После добавления обязательного канала появились ошибки:

```
WARNING  | Ошибка проверки подписки на @[: chat not found
WARNING  | Ошибка проверки подписки на @]: chat not found
WARNING  | Ошибка проверки подписки на @": chat not found
WARNING  | Ошибка проверки подписки на @@: chat not found
WARNING  | Ошибка проверки подписки на @R: chat not found
WARNING  | Ошибка проверки подписки на @i: chat not found
WARNING  | Ошибка проверки подписки на @c: chat not found
WARNING  | Ошибка проверки подписки на @h: chat not found
WARNING  | Ошибка проверки подписки на @i: chat not found
WARNING  | Ошибка проверки подписки на @s: chat not found
WARNING  | Ошибка проверки подписки на @C: chat not found
WARNING  | Ошибка проверки подписки на @a: chat not found
WARNING  | Ошибка проверки подписки на @b: chat not found
WARNING  | Ошибка проверки подписки на @i: chat not found
WARNING  | Ошибка проверки подписки на @n: chat not found
```

Это означало что код итерировал по каждому **символу** строки `["@RichisCabin"]` вместо элементов списка!

## Причина

В файле `handlers/user/tournaments.py` строка 229:

```python
for channel_username in tournament.required_channels:
```

Код использовал **RAW JSON строку** `tournament.required_channels` вместо **property** `tournament.required_channels_list`.

### Объяснение:

В модели `Tournament`:
- `required_channels` - это **строка** JSON: `'["@RichisCabin"]'`
- `required_channels_list` - это **property** который парсит JSON в список: `['@RichisCabin']`

Когда Python делает `for ch in '["@RichisCabin"]'`, он итерирует по символам:
- `[`, `"`, `@`, `R`, `i`, `c`, `h`, `i`, `s`, `C`, `a`, `b`, `i`, `n`, `"`, `]`

## Решение

### 1. Исправили использование property

**Было:**
```python
if tournament.required_channels:
    for channel_username in tournament.required_channels:
```

**Стало:**
```python
required_channels_list = tournament.required_channels_list
if required_channels_list:
    for channel_username in required_channels_list:
```

### 2. Заменили на модуль channel_checker

Для консистентности с остальными частями кода (handlers/user/teams.py) заменили старую логику проверки на использование модуля `utils/channel_checker.py`:

```python
from utils.channel_checker import check_all_channels_subscription, format_channel_url, format_channel_name

# Используем модуль channel_checker для консистентности
is_subscribed, unsubscribed = await check_all_channels_subscription(
    callback.bot,
    callback.from_user.id,
    required_channels_list
)

if not is_subscribed:
    # Создаем клавиатуру с кнопками подписки
    # ...
```

### 3. Добавили логирование в channel_checker

Добавили дополнительную проверку типа в `utils/channel_checker.py`:

```python
# Логируем тип и содержимое для отладки
logger.info(f"Проверка каналов: тип={type(channels)}, значение={channels}")

# Проверяем что это действительно список
if not isinstance(channels, list):
    logger.error(f"channels не является списком! Тип: {type(channels)}, значение: {channels}")
    # Пытаемся исправить если это строка JSON
    if isinstance(channels, str):
        import json
        try:
            channels = json.loads(channels)
            logger.info(f"Успешно распарсили JSON: {channels}")
        except Exception as e:
            logger.error(f"Не удалось распарсить JSON: {e}")
            return False, []
```

## Измененные файлы

1. **handlers/user/tournaments.py** (строки 224-258)
   - Заменено `tournament.required_channels` → `tournament.required_channels_list`
   - Заменена логика проверки на использование `channel_checker` модуля
   - Добавлены кнопки подписки на каналы

2. **utils/channel_checker.py** (функция `check_all_channels_subscription`)
   - Добавлено логирование типа и значения
   - Добавлена проверка что `channels` - это список
   - Добавлена попытка автоматического парсинга если передана JSON строка

## Проверка исправления

После исправления:
- ✅ Каналы отображаются правильно (не по символам)
- ✅ Проверка подписки работает корректно
- ✅ Кнопки для подписки на каналы добавлены
- ✅ Сообщения форматируются через HTML (консистентность)

## Предотвращение подобных ошибок

**Всегда используйте:**
- `tournament.required_channels_list` - для получения списка каналов
- `tournament.required_channels_list = [...]` - для установки списка каналов

**Никогда не используйте напрямую:**
- `tournament.required_channels` - это внутреннее поле для хранения JSON

## Тестирование

Для тестирования создан скрипт `test_subscription_debug.py` который:
1. Получает турнир из БД
2. Проверяет тип и значение `required_channels_list`
3. Запускает проверку подписки
4. Выводит детальную информацию о каждом этапе

Запуск:
```bash
python test_subscription_debug.py
```

Ожидаемый результат:
```
📢 Обязательные каналы:
   Тип: <class 'list'>
   Значение: ['@RichisCabin']
   Длина: 1

   Элементы списка:
     1. '@RichisCabin' (тип: str)
```

**НЕ должно быть:**
```
Ошибка проверки подписки на @[:
Ошибка проверки подписки на @R:
Ошибка проверки подписки на @i:
...
```
